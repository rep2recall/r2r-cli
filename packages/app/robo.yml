build:
  command: |
    go build --tags "{{ .buildTags }}" -o ../../dist/linux/{{ .exe }}
run:
  command: |
    robo build
    ../../dist/linux/{{ .exe }} $@

variables:
  buildTags: sqlite_fts5
  exe: '"$([ $(go env GOOS) = "windows" ] && echo "r2r.exe" || echo "r2r")"'
