build:
  command: |
    go build --tags "{{ .buildTags }}" -o ../../dist/{{ .exe }}
run:
  command: |
    robo build
    ../../dist/{{ .exe }} $@

variables:
  buildTags: sqlite_fts5
  exe: '"$([ $(go env GOOS) = "windows" ] && echo "rep2recall.exe" || echo "rep2recall")"'
